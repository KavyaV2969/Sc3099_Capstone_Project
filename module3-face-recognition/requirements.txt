fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
python-multipart==0.0.6
redis==5.0.1
httpx==0.26.0

# Face recognition & CV
# NOTE: mediapipe >= 0.10.30 removed the legacy `mediapipe.solutions` API
# (https://github.com/google-ai-edge/mediapipe/issues/6192) that this module
# uses (mp.solutions.face_detection / mp.solutions.face_mesh). 0.10.18 is the
# last release with mp.solutions that ships wheels for Linux x86_64 + aarch64,
# macOS and Windows on Python 3.9-3.12. Keep it pinned.
mediapipe==0.10.18
opencv-python>=4.9.0.80,<4.12  # 4.12+ requires numpy>=2, unsupported by mediapipe 0.10.x
numpy>=1.24.3,<2
pillow>=10.2.0
# face-recognition==1.3.0  # Optional - has Python 3.12 compatibility issues

# Observability
opentelemetry-api==1.22.0
opentelemetry-sdk==1.22.0
opentelemetry-instrumentation-fastapi==0.43b0
opentelemetry-exporter-otlp==1.22.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
